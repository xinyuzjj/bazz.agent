# BAZZ.AGENT v1.2.7

**Binance Agent OS 专属 AI 交易桌面端（Agent OS Alpha Scout · Track A）**

本地优先的币安 AI 交易桌面前端：妖币/点火雷达 + 多模型 Agent 对话 + 带止损止盈的交易方案 + CEX 连接 + Agentic Wallet 链上操作 + 广场发文。行情扫描与 Agent 记忆**不需要任何 API Key**；真实下单/写盘一律先确认后执行。

## 🆕 v1.2.7 更新要点

1. **全新启动动画（液态流体转场）**：双击 exe 后不再是黑屏干等，而是先呈现一段约 5 秒的品牌开场——暗底点阵上打出 ALPHA SCOUT → 币安金液态从右下爆开铺满全屏（带溅射金粒与冲击波圆环）→ 金屏上刷出 BAZZ.AGENT → 深色液态从顶部盖回，定格白字品牌 + 金色发丝线 + 副标，与主界面同色系无缝衔接。动画在核心引擎就绪后自动淡出，进入主界面。
2. **开场视觉对齐应用深色金融风**：近黑 `#0a0b0d` 底 + 币安金点缀 + 细点阵网格，全片零外部资源、纯 CSS/SVG 实现，不拖慢启动。

## 🔄 v1.2.6 更新要点（历史）

1. **设置页新增「软件更新」管理区**：设置 → 深度思考下方即见完整更新入口——当前/最新版本一目了然，「检查更新」一键手动检测；检测到新版本可直接在设置页下载（带进度）、查看更新说明并「重启并更新」。
2. **右下角悬浮提醒保留**：有新版本时依然会在桌面右下角浮出提醒卡（可「稍后」隐藏），与设置页管理入口互补。

## 🔄 v1.2.5 更新要点（历史）

1. **自动更新（Auto-Update）**：GitHub 发布新版本后，旧版客户端会自动收到提醒——右下角浮出「发现新版本」卡片（含更新说明与体积），点一下即可后台下载（带进度），下载完成后一键「重启并更新」：应用自动退出 → 原位整目录替换为新版 → 自动重启，全程无需手动。
2. **交易所历史成交不再要求搜代币**：进入 Binance CEX 页，历史成交自动聚合展示账户所有非零币种的最近成交（合并去重、按时间倒序、最多 50 条）；代币输入框降级为可选过滤器。
3. **内置更新通道直连 GitHub API**：更新检查绕过 Windows 系统代理干扰（避免代理把 API 打成 403），网络异常时静默降级不打扰使用。

## 🔄 v1.2.4 更新要点（历史）

1. **全界面英文支持（English UI）**：右上角可切换语言；英文模式下导航、设置、行情、钱包 Hub、CEX、技能库、广场、记忆、议会、审批弹窗等**全部界面文字**均为英文。
2. **Agent 对话同步英文**：界面语言随每条消息下传后端，英文模式下 Agent 的回复、行情分析、工具卡片与兜底话术一律以英文输出。
3. **前端文案全面词典化**：将 ~1160 处硬编码中文抽成 zh/en 双语词典，一次生成、双语同源。

## 🚀 快速开始（Windows 便携版）

1. 下载并解压 `BAZZ.AGENT-v1.2.7-win32-x64-portable.zip`
2. 运行 `BAZZ.AGENT.exe` —— 先播放启动动画，随后自动生成 `workspace/` 工作区，运行数据（会话库 / 钱包 profile / 广场台账 / 附件 / 报告）全部收口其中
3. 右上角配置 LLM（OpenAI 兼容端点，支持多模型 + 备份链），即可开始对话
4. 行情 / 妖币雷达 / 市场扫描开箱即用，无需任何 Key
5. 真实交易：设置 → 币安 CEX 填 API Key（应用内不落盘，可 sync 到 binance-cli profile）；或 Bots 面板扫码登录 Agentic Wallet（MPC 无密钥）——均需你在界面点确认才执行

## 🔒 安全设计

- 高危指令（全仓 / 清空账户等）硬拦截；下单 / 写盘默认二次确认，可信操作可勾选白名单
- 更新包仅从本仓库 GitHub Releases 下载，落盘后原位校验替换，不联网执行任何第三方脚本
- 所有运行数据仅存本机 `workspace/`，私钥 / 助记词永不上传

## 📖 更多文档

- 中文说明：`README.zh-CN.md` ｜ English：`README.md`
- 产品页：`docs/index.html`（可本地打开浏览）
