# BAZZ.AGENT v1.2.4

**Binance Agent OS 专属 AI 交易桌面端（Agent OS Alpha Scout · Track A）**

本地优先的币安 AI 交易桌面前端：妖币/点火雷达 + 多模型 Agent 对话 + 带止损止盈的交易方案 + CEX 连接 + Agentic Wallet 链上操作 + 广场发文。行情扫描与 Agent 记忆**不需要任何 API Key**；真实下单/写盘一律先确认后执行。

## 🆕 v1.2.4 更新要点

1. **全界面英文支持（English UI）**：右上角可切换语言；英文模式下导航、设置、行情、钱包 Hub、CEX、技能库、广场、记忆、议会、审批弹窗等**全部界面文字**均为英文，不再残留中文。
2. **Agent 对话同步英文**：界面语言会随每条消息下传后端，英文模式下 Agent 的回复、行情分析、工具卡片与兜底话术一律以英文输出（同时保留中文提问也能得到英文回答）。
3. **前端文案全面词典化**：将 ~1160 处硬编码中文抽成 zh/en 双语词典（本版新增 340+ 词条并统一补齐字典缺口），一次生成、双语同源，后续新增界面天然可切英文。
4. **稳定性修正**：修复批量转换中出现的若干变量遮蔽告警（对话工具卡 / DeFi 面板 / 定时任务 / 代码卡等渲染无副作用）。

## 🔄 v1.2.3 更新要点（历史）

1. **对话不再被「切页面」打断**：聊天视图常驻挂载，生成中切到行情/钱包/CEX 再切回，回复完整继续，不中断不丢失。
2. **合约行情直接可查，无需 MCP 授权**：U 本位永续 / 资金费率 / 合约代币一律走本地免费公开行情通道（scan_market / market_quote）；MCP 仅用于账户级私有数据（余额/持仓/真实下单）。
3. **支持纯永续上市币**：现货查不到的币（如 SNDKUSDT）自动回退 Binance fapi 公开接口，返回合约价量 + 资金费率。
4. **探索轮次加深、收尾更负责**：单次生成轮次上限 14；分析类请求补齐资金费率/量能/大盘强弱再给结论；多轮失败给出 4 条具体可换路径，不再空话收尾。

## 🚀 快速开始（Windows 便携版）

1. 下载并解压 `BAZZ.AGENT-v1.2.4-win32-x64-portable.zip`
2. 运行 `BAZZ.AGENT.exe` —— 首次启动自动生成 `workspace/` 工作区，运行数据（会话库 / 钱包 profile / 广场台账 / 附件 / 报告）全部收口其中
3. 右上角配置 LLM（OpenAI 兼容端点，支持多模型 + 备份链），即可开始对话
4. 行情 / 妖币雷达 / 市场扫描开箱即用，无需任何 Key
5. 真实交易：设置 → 币安 CEX 填 API Key（应用内不落盘，可 sync 到 binance-cli profile）；或 Bots 面板扫码登录 Agentic Wallet（MPC 无密钥）——均需你在界面点确认才执行

## 🔒 安全设计

- 高危指令（全仓 / 清空账户等）硬拦截；下单 / 写盘默认二次确认，可信操作可勾选白名单
- 所有运行数据仅存本机 `workspace/`，私钥 / 助记词永不上传

## 📖 更多文档

- 中文说明：`README.zh-CN.md` ｜ English：`README.md`
- 产品页：`docs/index.html`（可本地打开浏览）
