# Security Policy

感谢你关注 BAZZ.AGENT 的安全。本文档说明**支持的版本**、**如何报告漏洞**以及
**响应时间预期**。

本项目是币安 AI 交易桌面端（Electron + FastAPI + React），涉及**真实下单、
钱包签名、本机 API Key / OpenAPI Key 持久化、本机回环令牌等敏感资产**。
任何可能影响用户资金或凭据的漏洞，请**私下报告**，**不要公开 issue**。

## Supported Versions（受支持版本）

下表说明哪些版本会接收安全更新。**仅最新发布版本及其前一个版本**接收修复；
更早的版本请升级后再使用。

| Version | Supported          |
| ------- | ------------------ |
| v1.5.x  | ✅（当前 v1.5.61） |
| v1.5.x 上一位（v1.5.60） | ✅（仅限关键安全问题） |
| < v1.5.60 | ❌ 不接收修复，请升级到 v1.5.61 |

桌面端用户：请到「设置 → 关于」查看当前安装版本；v1.5.x 之外的旧版本请直接
升级安装包。

## Reporting a Vulnerability（如何报告漏洞）

**请勿**在 GitHub Issues / Discussions / Pull Request 中公开披露安全漏洞，
也不要提交能直接复现的最小利用代码到公开仓库。

请把报告发到：

- 邮件：**[security@bazz.agent](mailto:security@bazz.agent)**
- 邮件主题建议：`[SECURITY] <简短描述>`，便于优先分拣

报告内容请尽量包含：

1. **标题版本号**（如 v1.5.61）+ 安装包类型（setup.exe / 浏览器模式）
2. **影响面**：钱包签名 / 真实 API 调用 / 本机凭据 / 安装包更新链路 / 其他
3. **可复现步骤**（最小路径；**不要**附完整利用脚本）
4. **预期 vs 实际**：你看到的具体行为、是否影响真实资金
5. **环境信息**：Windows 版本、是否启用 Agent / 链上钱包、是否在交易时段复现
6. **PoC 视频 / 截图**：本地文件可附，不要直接贴密钥或私钥截图

PGP 不强制 —— 邮件本身已是私密通道。如果你要求加密回信，请告知，我们会
回复 PGP 公钥。

## What to Expect（响应预期）

| 阶段 | 预期 |
| --- | --- |
| 首次确认收到 | **48 小时内**（工作日） |
| 初步评估（严重性、影响范围） | **5 个工作日内** |
| 修复计划与时间表 | 初步评估后 **3 个工作日内** |
| 修复 Release | 按严重性：**关键 7 天 / 高 14 天 / 中 30 天 / 低 随版本** |
| 公开致谢 | 修复发布后随 Release Notes 公布（除非你要求匿名） |

修复发布前我们**不会**公开细节（包括该漏洞是否被报告过），但会告知你"正在修复"。

## Coordinated Disclosure（协调披露）

我们承诺：

- 收到你的报告后 **48 小时内**确认并给出严重性初步判断
- 修复前与你保持沟通；如需延长披露窗口（适配你的研究发布计划）会与你协商
- 修复发布后，**Release Notes** 与 **CHANGELOG** 中明确致谢（除非你要求匿名）
- 收到低质量或恶意报告仍会礼貌回信；不会因拒绝接受某些披露方式而忽略你

我们也希望你承诺：

- **不要**在修复发布前向第三方披露（除了为协调披露需要的同行/厂商）
- **不要**利用漏洞获取真实数据、真实资产、影响真实用户
- **不要**对系统做超出报告目的的访问、修改或破坏
- 给我们合理时间响应、修复与发布
- 如未能在约定时间窗内协商一致，双方再各自决定下一步

## Out of Scope（不在范围内）

以下**不是安全漏洞**，请发到普通 Issue / Discussions：

- 非最新版本上的问题（请先升级到 v1.5.61 再报告）
- 自我拒绝服务（DoS）需要本地 shell + 已登录用户
- 业务规则讨论（"为什么这个订单要二次确认"）
- 配置错误使用者的本地风险（用户在公共电脑上不锁屏导致凭据泄漏）
- 第三方组件已知问题（请同步给上游）

## Security Best Practices（安全最佳实践）

虽然不在漏洞报告范围，但本项目文档里有几条用户侧护栏值得了解：

- 本机回环令牌 `BAZZ_AUTH_TOKEN` 在 Electron 启动时随机生成；**不要**把它写进任何
  外部配置或 shell rc（避免被沙箱或脚本误读）
- 安装包内 `ScoutBackend.exe` 已编译，**不要**替换或注入 `_internal/*.pyd`
- 广场 OpenAPI Key / CEX API Key 通过 AES-256-GCM 加密落盘到 `~/.config/bazz-agent/`，
  **不要**分享 keystore 文件
- mihomo 内核进程由 `proxy_kernel` 拉起并写入 `state.json`；**不要**手动改这个文件
  （端口会被恢复逻辑清零，等于把代理关掉）
- 更新链路走专用退出通道 `bazz:quit-for-update`；**不要**点 X 关掉应用再升级

## Acknowledgements（致谢）

历史上报告过问题并被采纳的外部安全研究者，名单将随版本致谢公布。如果你报告过但
没有看到名字，请告知 —— 这不是遗漏。

## Contact

- 安全邮箱：[security@bazz.agent](mailto:security@bazz.agent)
- 一般问题：GitHub Discussions / Issue（公开）
- 项目主页：https://github.com/xinyuzjj/bazz.agent