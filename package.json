{
  "name": "agent-os-alpha-scout",
  "private": true,
  "version": "1.5.0",
  "description": "Agent OS Alpha Scout - Hermes-style Binance desktop agent (Electron + React, FastAPI backend)",
  "scripts": {
    "install:fe": "cd frontend && npm install",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "electron": "cd frontend && npm run electron",
    "start:dev": "start-dev.bat",
    "start:desktop": "start-desktop.bat"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "electron-packager": "^17.1.2"
  }
}
